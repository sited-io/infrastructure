[[ with secret "consul/creds/nomad_server_role" ]]
consul {
  address = "{{ localhost_ip }}:{{ consul.ports.http }}"
  grpc_address = "{{ localhost_ip }}:{{ consul.ports.grpc }}"
  token   = "[[ .Data.token ]]"
}
[[ end ]]
